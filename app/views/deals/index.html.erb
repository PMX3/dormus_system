<p id="notice"><%= notice %></p>

<h1>Listing Deals</h1>

<table>
  <thead>
    <tr>
      <th>Deal type</th>
      <th>Amount due</th>
      <th>Amount paid</th>
      <th>Deal timestamp</th>
      <th>Payment for</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @deals.each do |deal| %>
      <tr>
        <td><%= deal.deal_type %></td>
        <td><%= deal.amount_due %></td>
        <td><%= deal.amount_paid %></td>
        <td><%= deal.deal_timestamp %></td>
        <td><%= deal.payment_for %></td>
        <td><%= link_to 'Show', deal %></td>
        <td><%= link_to 'Edit', edit_deal_path(deal) %></td>
        <td><%= link_to 'Destroy', deal, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Deal', new_deal_path %>
